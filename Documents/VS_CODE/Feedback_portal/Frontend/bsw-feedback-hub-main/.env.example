# ── Required: copy this to .env.local and fill in values ─────────────────────
# ⚠️  All VITE_ variables are embedded in the browser bundle — do NOT put
#     secrets, API keys, or tokens here.

# URL of the Express backend
VITE_API_BASE_URL=http://localhost:4000
